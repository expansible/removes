%YAML 1.1  # uninstalls/defaults/main.yml  -*- mode: yaml -*-

# Default values for uninstalls
---
# packages to uninstall
removes:

# packages to explicitly install to prevent autoremove
reinstalls:
